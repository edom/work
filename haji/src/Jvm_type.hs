{- |
Consider using "Meta.JvmType" instead.

This module should be imported qualified because some names clash with those in "Jvm_value".
-}
module Jvm_type (
    module Meta.JvmType
) where

import Meta.JvmType
