# There doesn't seem to be any way to source a path relative to gdbinit path.

source ~/work/etc/gdb_pcsx

set pagination off
set history save on
set history size 1024
set history filename ~/.gdb_history
