include 'concurrent'
include 'concurrent:lock'
include 'concurrent:lock-server'
include 'io'
include 'java-build'
include 'java-build:plugin:gradle'
include 'server'
include 'web-all'
include 'web-example'
include 'web-example-http-server'
include 'web-example-json-api'
include 'web-http'
include 'web-http-server'
include 'web-server'
include 'web-servlet'
include 'web-servlet-json'
include 'web-view'

project(':concurrent').children.each { p ->
    p.name = "concurrent-${p.name}"
}

project(':java-build:plugin').children.each { p ->
    p.name = "java-build-plugin-${p.name}"
}

project(':java-build').children.each { p ->
    p.name = "java-build-${p.name}"
}
